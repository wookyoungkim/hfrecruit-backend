spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/test?useSSL=false&useUnicode=true&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=utf-8
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: sallydusdn98
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  profiles:
    include: oauth
  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: RRmaWc3aGqrvUcYP2rTE
            client-password: l7GxrKFhpg
            scope:
              - name
              - emaiL
              - profile_image
            client-name: Naver
          provider:
            naver:
              authorization_uri: https://nid.naver.com/oauth2.0/authorize
              token_uri: https://nid.naver.com/oauth2.0/token
              user-info-uri: https://openapi.naver.com/v1/nid/me
              user_name_attribute: response